//Test program for SPI and I2C in RPI

#include "mcp3008.h"

int fd = -1;

int main(int argc, char**argv)
{
   fd = init_mcp3k8(SPI_DEV_0,1000000,SPI_MODE_0,8);
   for(int i=0;i<100;i++){
      printf("MCP3008 CH0 : %.2f\n",read_channel_mcp3k8(3.3,0));
   }
   close_mcp3k8(fd);
   return 0;
}
